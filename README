Welcome to Kwiki 2.0

For a fast start, change directory:

    cd server/
    more README
    make

== Installation

0 Run `make` in this directory
0 Put `./bin/` in your path
0 Run `kwiki -new mywiki` somewhere


